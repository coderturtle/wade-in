# Decisions: Wade In

## ADR Log

| Date | Decision | Rationale |
|---|---|---|
| 2026-09-14 | Initial scaffold as factory-output | Teach non-technical knowledge workers, who have never used a terminal, to get real research/document/spreadsheet-adjacent/light-automation work done with Claude Code itself, gated by a deterministic checker script plus an AI-graded rubric, inside one continuing named scenario. |
| 2026-09-14 | Workshop named **Wade In**, chosen by the human from 8 GitHub-slug-checked candidates (Wade In, First Command, Keyboard Native, Cold Start, Blank Terminal, Console Native, The Lift, Dirty Terminal) | Doubles as a Road House (Wade Garrett) callback extending the factory's Swayze-verse in-joke pattern, and as the literal "ease in gently" promise to a learner with zero prior terminal experience. Local dir, private sibling, GitHub repo, `.hekton/project.yaml`, vault card, and repo-local mind-palace mirror renamed same day (branch `agent/claude/naming-rename`, commit `150f1fd`). |
| 2026-09-14 | Audience scope: zero prior terminal experience (harder floor than `copilot-fluent`); Module 01 owns terminal literacy itself rather than outsourcing to an external primer | Confirmed via an `interview-me` pass before any drafting began. Claude Code's own interface is the subject, so unlike `copilot-fluent` (which could anchor to already-trusted M365 apps), there is no existing trusted surface to anchor to — the workshop has to build the floor itself. |
| 2026-09-14 | In-scope capabilities: file read/edit, running commands (including Claude-authored scripts the learner directs/verifies but never reads/writes by hand), web research, file-based automation/templating. Out of scope: git, subagents/Task, MCP, any learner-authored-code exercise | Confirmed via the same interview. The line against learner-authored code specifically distinguishes this from a programming workshop. |
| 2026-09-14 | Scenario vehicle: one continuing named scenario (The Double Deuce, a *Road House*-themed venue), reusing the factory's Swayze-verse pattern but with a distinct, non-Kellerman-&-Castle cast | Confirmed via interview: a single continuing scenario fits a cross-functional role (research/documents/spreadsheets/automation) the same way it fit `copilot-fluent`'s narrower financial-advisory role. |
| 2026-09-14 | Two-tier gate: primary = a real, locally-executable deterministic checker script (closer to `borrow-native`'s `cargo test` tier than to `copilot-fluent`'s hidden-key checklist); secondary = an AI-graded rubric graded by Coachgremlin (not the product self-grading) | Confirmed via interview. Unlike a closed SaaS product, Claude Code's own harness can run real scriptable checks against the learner's real files — judged a genuine opportunity to ship a stronger gate than copilot-fluent's, not just its substitute. Checker hardening (neutral temp dir, checksum-verified fixtures, no natural-language phrase checks) designed in from the start per this factory's own `closed-book` checker-bypass lessons. |
| 2026-09-14 | Safety model: sandboxed by default for the whole graded arc; real-directory work taught as an explicit, late-arc skill (Module 09), never assumed safe from Module 01 | Confirmed via interview. Named as this workshop's own first-class safety layer, parallel in seriousness to `copilot-fluent`'s liability guardrails but addressing a different real risk (real shell/file access vs. a sandboxed SaaS chat). |
| 2026-09-14 | No external certification anchor. Anthropic's Claude Certification (Architect track, `closed-book`'s own anchor) and `closed-book`'s content are informal internal reuse material only, never presented to the learner as a target | Confirmed via interview: the human's own words were that Claude Certification is "more engineer focused" than this audience. Unlike AB-730 for `copilot-fluent`, no known real published blueprint exists for this audience; success is defined entirely by this workshop's own module arc. |
| 2026-09-14 | Keepable takeaway: a personal `CLAUDE.md` plus a reusable prompt/script pack, assembled module by module, capped at a synthesis capstone | Confirmed via interview as the literal artifact of "fluent." Not yet fully gate-coupled per Design Principle 4 — see `docs/workshop-design.md` §7's named gap, open for content time. |
| 2026-09-14 | Format: self-paced public repo with the published site as primary learner surface, same shape as `copilot-fluent`, but with an explicit three-way split (site reads / terminal does / Claude Code's file output is the artifact) named as a real structural difference | Confirmed via interview. |
| 2026-09-14 | `docs/workshop-design.md` drafted via the three-agent review chain (Fable plan → Codex adversarial critique with live repo access, 21 confirmed findings → this reconciliation pass) and reconciled same day | Matches `copilot-fluent`'s and `heartbeat`'s own precedent for this pattern. Full critique preserved at `wade-in-private/docs/codex-critique-workshop-design-2026-09-14.md`; every CONFIRMED finding was fixed in the reconciled document, and several open design questions (Tier-2's completion contract, the platform decision, the CLAUDE.md file-identity ambiguity, the Design Principle 4 gap) were named as genuinely still-open rather than papered over. |
| 2026-09-14 | Workshop Review Panel (7-persona pass) run against the reconciled design doc; findings applied same day, design doc restructured into 15 sections (added §14 Open design questions, §15 Revision Log) | Per `~/hekton/gremlins/workshop/workshop-review-panel.md`'s process, personas fanned out independently with the End-User/Target Learner persona re-aimed to this workshop's actual zero-terminal-experience audience. Full report at `docs/review-panel/2026-09-14-initial-design.md`. Strongest finding, corroborated by 4 independent sources (3 personas + the earlier Codex pass): a Module 01→02 authenticated-session gap, promoted to §14 item 1 as blocking for content authoring. A second 4-persona convergence (the review chain's own correction-callout apparatus was hurting readability and risked leaking maintainer vocabulary) drove the §14/§15 restructure directly. |
| 2026-09-14 | Deliverables & branding step complete: 10-module skeleton (`modules/`), brand layer (`docs/brand.md`), `docs/maintainers.md` split from `README.md`, `scripts/check-brand-lint.sh` wired into the pre-push hook | Module READMEs follow the Workshop Gremlin's 8-part template (question, arc position, placeholder objectives, exercise-material pointer, required gate stated with the deterministic tier first, takeaway, stop condition, skeleton banner). Per the Workshop Gremlin's voice-finding handoff, the Review Panel's Skeptical Practitioner findings (no unhedged "first...in this factory" framing, no unearned hype labels, hedge honestly not performatively) and the 4-persona vocabulary-leak convergence were both encoded as permanent hard rules in `docs/brand.md`, stricter than any prior workshop's brand layer on the maintainer-vocabulary point specifically (a hard exclusion from learner-facing copy, not `object-lesson`'s gloss-and-allow). |
| 2026-09-14 | `.gitignore` deviates from `scaffold-project.sh`'s default: `scripts/check-mirror-drift.sh` and `scripts/setup-hooks.sh` are tracked, not ignored | Matches `object-lesson`'s own 2026-08-17 precedent for the identical bug: the scaffold's git-init step only ever installs the pre-push hook from the public repo's own `scripts/`, so the template default (ignoring both files) means the hook never installs for any public-capable project. Both scripts are generic (no vault paths, no secrets). The underlying `scaffold-project.sh` bug itself is not fixed here (out of this workshop's scope) — tracked in `docs/next-actions.md` as a cross-workshop item, same class of bug as `copilot-fluent`'s already-flagged `verify-project.sh --dry-run` failure on `docs/local-assumptions.md`. |
| 2026-09-14 | Build-log/Pages publisher step complete: Astro site scaffolded under `site/` (adapted directly from `copilot-fluent`'s own site, the only prior workshop with the module-publishing + vocabulary-leak-placeholder pattern this workshop also needs), `docs/build-log/` started, deploy workflow defined (`workflow_dispatch` only, brand lint blocking) | No fixtures content collection yet (none exist — copilot-fluent only added its own once Modules 01-03 had real fixtures; adding one speculatively against nothing was rejected). `npm install`, `npm run build`, and `npx astro check` all run and clean; the vocabulary-leak placeholder verified against the actual built HTML output (`dist/modules/01-.../index.html` contains no "skeleton," "Tier 1," or "required checklist" text). `npm audit` found 5 vulnerabilities on the same shared `astro@^5.0.0` pin `heartbeat`/`copilot-fluent` already flagged as a cross-workshop decision — not force-upgraded unilaterally, logged in `docs/risks.md` RISK-0009. No deploy triggered; GitHub Pages not yet enabled in repo settings — both explicitly deferred until real module content exists, per this workshop's own design doc status line. |
| 2026-09-14 | Design Principle 5 cross-model pass #2 (Codex, against the Deliverables/branding and Build-log/Pages publisher steps, 13 confirmed findings) reconciled same day | Full critique at `wade-in-private/docs/codex-critique-deliverables-site-2026-09-14.md`; two of its most concrete findings (a real truncated-question bug in the built site, and 48 unbanned "checker"/"the arc" occurrences flagged against an over-broad first draft of `docs/brand.md`'s own vocabulary rule) independently spot-verified before applying the rest. Fixed: the site's question-extraction regex (was silently truncating any module Question that wrapped across two source lines — confirmed live on Modules 04/05/06/10); `scripts/check-brand-lint.sh` adopted `copilot-fluent`'s superior multiline-paragraph-join scan and `brand-lint-ignore` allowlist marker (both real hardening the first draft lacked) plus two missing banned phrases; `docs/brand.md`'s maintainer-vocabulary rule narrowed to match the linter's actual, deliberate scope (ordinary words "checker"/"the arc" aren't real jargon leaks) rather than the linter widened to match an over-broad rule; several present-tense-for-unbuilt-machinery and absolute-claim-contradicts-Module-05/09 violations across `README.md`, `modules/README.md`, and the site homepage; all ten module skeletons tightened to restore the specific file paths, exact counts, and header names `docs/workshop-design.md` §7 actually specifies, which had been lost to paraphrase; two "hedge stated then treated as settled" instances (Module 01's unhedged "no prior Hekton workshop" framing, Module 10's stop condition). **Deliberately not fixed:** Part 4 (exercise-material pointers) staying generic for Modules 02-10 — real curriculum-source research is named, upstream, as Coachgremlin's later job in `docs/workshop-design.md` §9's own admission that this design pass didn't do a live landscape survey; doing it now would be scope creep into content-authoring, not a Deliverables-step defect. Tracked as a real, acknowledged gap in `docs/next-actions.md`, not silently left. |
| 2026-09-14 | Module 01 authored for real: learner content, fixtures, and a working checker script — the first genuine content this workshop has ever shipped | Preceded by a live-verification pass against Anthropic's own current docs (`code.claude.com/docs/en/setup`, `.../terminal-guide`, `.../authentication`), which resolved both of the design doc's top blocking questions for real: §14 item 1 (Module 01 owns install-and-login; Module 02's own checker is the genuine auth proof) and §14 item 2 (all three platforms supported natively, not WSL-first as previously assumed). A fresh-context doubt-driven-development pass followed, given only the artifact and the design contract — found and fixed two real checker bugs (a false "fixture-tampering resistance" claim, a demonstrated symlink bypass, both directly reproduced then re-verified fixed), a genuine Windows `bash`-dependency gap, a download-instructions folder-naming mismatch, an unglossed brand-rule term, and a Tier-2/design-doc mismatch (resolved by deciding Module 01 genuinely doesn't need an AI-graded rubric, not by retrofitting one to match a pre-authoring guess). Full account: `docs/workshop-design.md` §15 Revision Log, `wade-in-private/runs/2026-09-14-module-01-dry-run/`. |
| 2026-09-14 | Codex cross-model adversarial review of Module 01 run and reconciled: 13 confirmed findings, all applied | Two of the most load-bearing (a blocking `mkdir` command bug, and `claude auth status`'s existence as a real mechanical login check) were independently reproduced before trusting the rest. Six more real checker bypasses were found that the earlier same-model DDD pass missed entirely (symlinked-parent-dir, hard-link, placeholder-answer, unchecked-login, fake-executable, and a missing-checksum-tool misdiagnosis). Login gating moved from a self-attested answer field to the real `claude auth status` mechanical check. `workshop-folder/CLAUDE.md` created for real, closing a gap where §8 had claimed it existed since before any content was authored. `scripts/check-brand-lint.sh` extended to scan `workshop-folder/` content, which immediately caught two real em dashes in the new `CLAUDE.md`. One fix (the checksum-tool diagnosis) was itself found incomplete on re-test after being marked fixed — corrected, and named as a reminder that "fixed" claims need direct re-testing, not just re-reading. Full account: `docs/workshop-design.md` §15 Revision Log, `wade-in-private/docs/codex-critique-module-01-2026-09-14.md`, `wade-in-private/runs/2026-09-14-module-01-dry-run/codex-followup.md`. Still open before this module is considered ship-ready: a Mock Learner Gremlin run (this factory's first on a workshop other than `copilot-fluent`), real Linux/Windows testing of the checker (macOS-only so far), and committing `workshop-folder/` itself (currently untracked — RISK-0012). |

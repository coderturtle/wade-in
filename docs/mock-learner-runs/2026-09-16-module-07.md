# Mock Learner Gremlin: Module 07 (Penny's Ledger)

**Date:** 2026-09-16
**Two personas:** Denise (diligent), Marcus (scripted-flaw). Both given only `MODULE-README.md` and a fresh copy of `workshop-folder/`. Verification performed directly by the orchestrator.

## Denise (diligent) — PASS 8/8, thorough beyond the minimum

Real nested `claude -p` call correctly merged 11 unique booking IDs with Penny's file winning on the 3 real conflicts (BK-103, 104, 105). Denise's own hand-verification went beyond what Part 3 requires: the module only asks to verify one conflicting ID, but she independently found and noted all three real conflicts, and specifically double-checked BK-106 (Founders Day Reunion - the exact booking Penny named by name as the one she'd notice going missing). Checker: `PASS (8/8)`, independently reconfirmed.

## Marcus (scripted-flaw) — FAIL 7/8, gate discriminated with surgical precision

**Scripted flaw:** a literal, concrete mistake baked into his own request - Marcus misremembered which file was authoritative and explicitly told Claude Code "use Garrett's row - his file is authoritative on conflict," the exact opposite of Penny's real, stated rule.

**What happened:** the real nested Claude Code invocation faithfully executed the flawed instruction it was given - correctly identified all 11 unique IDs, correctly preserved every single-source row, correctly computed every `total_due`, and correctly used Garrett's (wrong) values on the three real conflicts. This is a clean, isolating result: every mechanism *except* the one under test worked perfectly, so the checker's single `FAIL` lands exactly on the conflict-resolution rule and nothing else - `RESULT: FAIL (7/8)`, independently reconfirmed.

**Why Marcus's own verification missed it:** his Part 3 row-count check (11 unique IDs by hand, 11 rows in the file) passed cleanly, because total row count is invariant to *which side* wins a conflict - only checking the per-row *values* on a conflicting ID would have caught it, and Marcus, believing he already had the rule right, checked a single-source row (BK-109) instead of re-verifying a conflicting one. This is a precise, realistic illustration of the module's own point: "don't take the row count on faith" is necessary but not sufficient - the count can be perfectly right while the values are perfectly wrong.

## What this run establishes

This is one of the cleanest discrimination results in the sweep: a scripted flaw isolated to exactly one mechanism (conflict-resolution direction) produced a checker result isolated to exactly one failing check, with every other check passing on real, correctly-computed data. The gate's design - checking row count, ID-set membership, per-field values, and computed totals as four separate, independent checks rather than one blended "does it look right" check - is exactly what makes this precision possible; a coarser check could easily have missed that everything else was correct.

## Honest limit

Same permission-mode adaptation as every prior module - neither persona experienced Module 07's real live permission prompts.

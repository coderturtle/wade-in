# Next Actions: Wade In

## Immediate

- [x] Define project brief — see `.hekton/project.yaml` idea field
- [x] Record first design decisions in `docs/decisions.md` — naming pass + full interview-confirmed scope now logged
- [x] Draft, adversarially critique, and reconcile `docs/workshop-design.md` (three-agent chain) — done 2026-09-14
- [x] Run the Workshop Review Panel (7-persona pass) against the reconciled `docs/workshop-design.md` — done 2026-09-14, full report at `docs/review-panel/2026-09-14-initial-design.md`, findings applied to `docs/workshop-design.md` §14/§15
- [x] Deliverables & branding: 10-module skeleton (`modules/`), brand layer (`docs/brand.md`, `docs/maintainers.md`, `docs/risks.md` populated), `scripts/check-brand-lint.sh` written and passing clean, wired into the pre-push hook — done 2026-09-14
- [x] Design Principle 5 cross-model pass #2 (Codex, against Deliverables/branding + site skeleton, 13 confirmed findings) — done 2026-09-14, all applied except the deliberately-deferred Part 4 item below; see `docs/decisions.md`

## This Week

Design doc §14 items, in its own priority order:

- [x] Close the Module 01→02 authenticated-session gap — resolved 2026-09-14 via live verification of Anthropic's own docs. §14 item 1, §11, RISK-0002 (now closed).
- [x] Resolve the platform decision — resolved 2026-09-14, all three platforms in scope from Module 01, verified live. §14 item 2, §11, RISK-0003 (now closed).
- [x] Install-step placement — resolved 2026-09-14 by actually authoring Module 01 (install is Part 3 of 5). §14 item 6.
- [ ] Pin Tier 2's completion contract: required-to-advance status, submission convention, model policy, disagreement/retry path. §14 item 3, RISK-0005. **Partial data point from Module 01 (2026-09-14): not every module needs a Tier 2 — Module 01's own three questions are basic terminology reflection, not judgment calls, so it ships with no AI-graded rubric at all, a real considered decision not a placeholder. This narrows but doesn't resolve the broader question for modules that do have real judgment content (04, 06, 09, etc.).**
- [ ] Decide whether each module's checker should also gate that module's own `my-pack/` contribution (the Design Principle 4 gap). §14 item 4.
- [ ] Decide whether the safety-instructing `CLAUDE.md` and Module 03's learner-editable `CLAUDE.md` are the same file or two. §14 item 5.
- [ ] Decide whether the sandbox should also be enforced via Claude Code's own permission-mode/allowed-directory settings, not instruction alone. §14 item 7.
- [ ] Decide whether a permission-habituation counter-exercise should land earlier than Module 09. §14 item 8.

## Module 01 content-authoring status

- [x] Real content, fixtures, and checker script authored 2026-09-14 (`modules/01-the-back-office/README.md`, `workshop-folder/`). Fresh-context doubt-driven-development pass run same day — found and fixed two real checker bugs (a false "fixture-tampering resistance" claim, a symlink bypass), a Windows `bash`-dependency gap, a download-instructions folder-naming mismatch, an unglossed term, and the Tier-2 mismatch above. See `docs/workshop-design.md` §15 Revision Log and `wade-in-private/runs/2026-09-14-module-01-dry-run/`.
- [x] Codex cross-model review of Module 01, per this workshop's own three-agent-chain pattern — done 2026-09-14, 13 confirmed findings, all applied. Full critique at `wade-in-private/docs/codex-critique-module-01-2026-09-14.md`, fixes and re-verification at `wade-in-private/runs/2026-09-14-module-01-dry-run/codex-followup.md`. Two of the most load-bearing findings (the blocking `mkdir` bug, `claude auth status`'s existence) were independently reproduced before trusting the rest. One finding (#7, missing-checksum-tool diagnosis) turned out not to be fully fixed on first pass — caught by re-testing directly rather than trusting the "already fixed" claim, and fixed for real this round.
- [x] Mock Learner Gremlin run on Module 01 — done 2026-09-14, the first real run of that gremlin against a workshop other than `copilot-fluent`, the cross-workshop milestone its own Follow-Up Actions had been waiting on. Run manually (not the Workflow tool): two blind personas (diligent, scripted-flaw), both independently verified. Both passed cleanly, mechanically and by independent human-grader read, with no self-check/verification disagreement — a stronger starting position than most `copilot-fluent` modules had on their first run, likely reflecting the two prior adversarial-review rounds this checker already survived. One real, previously-undetected finding: Part 5 lacked explicit troubleshooting text for a wrong-directory mistake (unlike Parts 3-4), found via the scripted-flaw persona's real recovery attempt; fixed with a one-line `pwd` sanity check. Full report: `docs/mock-learner-runs/2026-09-14-module-01.md`.
- [ ] The checker (`workshop-folder/checks/check.sh`) has only been tested on macOS. Linux and Windows execution (including whether the `sha256sum`/`shasum` fallback actually works on each, and whether `bash checks/check.sh 01` runs correctly from Git Bash on Windows) remain genuinely untested, not just unverified in prose — a real gap before any real learner on those platforms should be trusted.
- [ ] The downloadable-workshop-folder packaging pipeline still doesn't exist — Module 01 currently tells learners to download the whole GitHub repo and find `workshop-folder/` inside it, a real but clunky workaround, not the "one-click download" the design intends.
- [ ] `workshop-folder/` itself is still untracked as of this round's edits — commit it on the current branch before merging, or the download instruction points learners at something that doesn't exist in the public ZIP. Flagged by Codex's review, finding not-yet-committed.
- [ ] A genuinely fresh-account, clean-credential-store first login has not been tested (Codex's review flagged this too) — not run this round since it requires destroying a real local login state; worth doing once a disposable test account exists.
- [x] Build the site skeleton (Astro, vocabulary-leak-safe placeholder for skeleton-only modules per the non-engineering variant's standing safeguard) — done 2026-09-14, `npm run build` and `astro check` both clean, placeholder verified against real built HTML (no "skeleton"/"Tier 1"/"required checklist" leaks); no deploy triggered, per the Human Gate
- [ ] Cross-workshop: fix `scaffold-project.sh`'s default `.gitignore` so `scripts/setup-hooks.sh`/`scripts/check-mirror-drift.sh` aren't ignored by default in public-capable repos — same bug `object-lesson` found and worked around 2026-08-17, worked around again here 2026-09-14 (`docs/decisions.md`, `docs/risks.md` RISK-0008), still not fixed upstream
- [ ] `npm audit` on `site/`'s fresh install found 5 vulnerabilities (1 critical, 1 high) on the shared `astro@^5.0.0` pin every workshop site in this factory uses — `npm audit fix --force` wants a breaking major-version bump; same cross-workshop dependency decision `heartbeat`/`copilot-fluent` already flagged rather than fixed per-repo, not resolved here either. See `docs/risks.md` RISK-0009
- [ ] Get a human to enable GitHub Pages (source = GitHub Actions) and trigger `wade-in`'s first `workflow_dispatch` deploy, once real module content exists — not before, per `docs/workshop-design.md`'s own status line
- [x] All 4 Build-phase PRs merged to `main` 2026-09-14 (#4 naming-rename, #1 workshop-design-doc, #5 deliverables-branding [re-opened after #2 was auto-closed by a stacked-PR base-branch deletion], #3 site-skeleton). Found and fixed a real scaffold quirk along the way: `gh repo create` from `scaffold-project.sh` had set the repo's GitHub default branch to `agent/claude/naming-rename` (the first branch ever pushed to the empty repo) instead of `main` — GitHub auto-assigns the default branch to whichever branch lands first when a repo is created empty and `main` isn't pushed immediately. Fixed here via `gh api -X POST repos/coderturtle/wade-in --field default_branch=main`. Worth a `scaffold-project.sh` fix (push `main` first, or explicitly set the default branch) so a future workshop doesn't hit the same silent trap — not fixed upstream in this pass.
- [ ] Content-authoring time: give each module's Part 4 ("Exercise material to draw from") a real, specific pointer the way `object-lesson`'s own skeleton does (named books/tutorials/exercises), not the current generic pointer back to `docs/workshop-design.md` §7 — deliberately deferred out of this pass per Codex's cross-model finding, since real curriculum-source research is Coachgremlin's job, not a Deliverables-step task
- [x] **Separate, unrelated finding surfaced mid-session:** `copilot-fluent`'s live site (copilot-fluent.coderturtle.io) was stale — its only deploy (2026-09-12) predated the real content-authoring pass (commit `e6f2fda` and the Mock Learner Gremlin dogfood commits, merged after). User confirmed a redeploy; `gh workflow run deploy-pages.yml --repo coderturtle/copilot-fluent` triggered successfully 2026-09-14 after switching to the `coderturtle` `gh` account (the first attempt hit a wrong-account permissions error, not the classifier as first assumed).

## Later

- [ ] Live-verify every Claude Code product claim in `docs/workshop-design.md` before it reaches learner-facing copy: install/auth flow, permission-prompt behavior, web search/fetch availability, data-handling/retention policy, current OS/Windows support (§2, §4, §11, §12 all carry claims flagged not-yet-verified)
- [ ] Set up test machines for each target platform and a burner learner-grade Anthropic account for realistic cost/limit measurement
- [ ] Design the downloadable-workshop-folder packaging pipeline (no-git audience — never a `git clone`)
- [ ] Content-authoring pass (Coachgremlin, one module at a time): author, fresh-context DDD pass, cross-model adversarial review, Mock Learner Gremlin per module (manual orchestration preferred over Workflow-tool parallelization, per that gremlin's own documented reliability findings)
- [ ] Recruit and run at least one real human terminal-first-timer pilot before public launch — the only mechanism that can validate Module 01's on-ramp at all (`docs/workshop-design.md` §10)
- [ ] Deployment/DNS (agentic-infra-lab's `github-pages-dns` pattern) — explicitly deferred until real content exists
